{
  "name": "@jo51yon/claudia-shell",
  "version": "1.0.0",
  "description": "Claudia shared dashboard-shell UI: a DB-driven sidebar (Shell) and a resolveTab helper, extracted from the pattern proven on Claudia's own dashboard. See docs/_platform/shell-pattern-scoping.md in the claudia repo.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "style.css"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build"
  },
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0"
  }
}
